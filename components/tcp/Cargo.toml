[package]
name = "tcp"
version = "0.1.0"
edition = "2021"

[dependencies]
api = { path = "../../api" }
tokio = { version = "1.12", features = ["net", "sync", "io-util"] }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
